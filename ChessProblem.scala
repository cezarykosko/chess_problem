object ChessProblem {

  def main(args: Array[String]) = {
    //test of the estimated desired length of the algorithm
    for {
      i <- 1 to 49 //queen on any from all available fields
      j <- 1 to 30 //as above (estimates from now on)
      k <- 1 to 16 //bishop - as above
      l <- 1 to 12 //as above
      m <- 1 to 11 //king - as above
      n <- 1 to 10 //as above
      o <- 1 to 9  //knight
    } {}
  }
}

